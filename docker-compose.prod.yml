name: sunlife-bmi-2026-sli

services:
  app:
    image: rasyabent/sunlife-bmi-2025_sli:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    build: !reset null
    networks:
      - internal
      - web
